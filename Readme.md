# CLI Contact Book

A simple CLI tool built with Node.js to manage contacts — add, update, delete, and search from a local JSON file.

## Features

- Add new contacts
- List all contacts
- Update or delete a contact
- Search contacts by name or number